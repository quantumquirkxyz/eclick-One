{
  "name": "@eclick-one/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/collector-agent.ts",
    "dev:compliance": "bun --env-file=../../.env --watch src/compliance-agent.ts",
    "start:collector": "bun --env-file=../../.env src/collector-agent.ts",
    "start:compliance": "bun --env-file=../../.env src/compliance-agent.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@eclick-one/shared": "workspace:*",
    "viem": "^2.55.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.0"
  }
}

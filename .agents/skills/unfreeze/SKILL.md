---
name: unfreeze
description: "Removes the directory edit restriction set by /freeze or /guard. After /freeze <directory>, running /unfreeze allows edits across the entire codebase again."
---

# /unfreeze — Unlock

Removes the directory edit restriction set by `/freeze` or `/guard`.

After `/freeze <directory>`, running `/unfreeze` allows edits across the entire codebase again.

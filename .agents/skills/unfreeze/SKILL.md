---
name: unfreeze
description: "Removes the directory edit restriction set by /freeze or /guard for eclick One. After /freeze apps/api, running /unfreeze allows edits across the entire codebase again."
---

# /unfreeze — Unlock

Removes the directory edit restriction set by `/freeze` or `/guard`.

After `/freeze apps/api`, running `/unfreeze` allows edits across the entire codebase again.
